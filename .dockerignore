# Deny everything by default — only the items explicitly re-included below
# are sent to the Docker build context.  This prevents any new top-level
# directory from silently bloating the image.
*

# Application source — the only items the Dockerfile actually COPYs.
!core/
!routers/
!requirements.txt
!zerobeacon_mf_1000_main.py
